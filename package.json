{
  "name": "mcpserver-in-production",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "node --check lib/site.js && node --check api/render.js && node --check src/data/servers.mjs && node --check scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "verify:build": "node scripts/verify-build.mjs",
    "verify:routes": "node scripts/verify-routes.mjs",
    "verify:seo": "node scripts/verify-og-canonical.mjs",
    "verify:sitemap": "node scripts/verify-sitemap-integrity.mjs",
    "verify:preview": "node scripts/verify-preview-noindex.mjs",
    "verify:jsonld": "node scripts/verify-jsonld.mjs",
    "verify:links": "node scripts/verify-internal-links.mjs",
    "verify:claims": "node scripts/verify-no-placeholder-claims.mjs",
    "verify:redirects": "node scripts/verify-redirects.mjs",
    "build:redirects": "node scripts/build-redirects.mjs",
    "extract:old-urls": "node scripts/extract-old-urls.mjs",
    "verify:all": "npm run verify:build && npm run verify:routes && npm run verify:seo && npm run verify:sitemap && npm run verify:preview && npm run verify:jsonld && npm run verify:links && npm run verify:claims && npm run verify:redirects",
    "production:gate": "npm run typecheck && npm test && npm run build:redirects && npm run build && npm run verify:all"
  }
}
